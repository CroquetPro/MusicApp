<a href="<%= album_url(@track.album_id) %>">Back to the Album</a>

<h1> <%= @track.title %> </h1>
<a href="<%= edit_track_url(@track) %>">Edit track</a>
<%= button_to "Delete track", track_url(@track), method: "delete" %>
<br>
<h2><%= @track.band.name %> - <%= @track.album.name %></h2>

<p>
  <h4>Lyrics</h4>
  <br>
  <%= @track.lyrics %>
</p>

<%= render "notes/form" %>

  <a href="<%= new_album_track_url(@track.album_id) %>">New Track</a>
